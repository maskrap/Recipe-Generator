<div>
  <h3>{{recipe.name}}</h3>
  <h5>{{recipe.cuisine}} | {{recipe.dish}}</h5>
  <h4>{{recipe.time}} min. | {{recipe.serves}} ppl.</h4>
  {{recipe.ingredients}}
  {{recipe.instructions}}
  <img src={{recipe.image}} alt={{recipe.name}} class="admin-recipe"/>


  <button class="btn delbtn" {{action 'delete' recipe}}>Delete</button>
</div>
