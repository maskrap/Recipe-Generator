<div>
  {{recipe.name}}
  {{recipe.cuisine}}
  {{recipe.dish}}
  {{recipe.time}}
  {{recipe.serves}}
  {{recipe.ingredients}}
  {{recipe.instructions}}


  <button class="btn delbtn" {{action 'delete' recipe}}>Delete</button>
</div>
